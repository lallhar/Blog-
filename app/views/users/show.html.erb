<h1 align="center"> Welcome to <%= @user.username %>'s page </h1>

<div class="row">
	<div class="col-md-4 offset-md-5 center">
		<%= gravatar_for @user, size: 180 %>
	</div>
</div>

<h4 align="center"><%= @user.username %>'s Article</h4>
 
<%= render 'articles/article', obj: @user.articles %> 